import {store} from "./store.js"
 const reducer = (store,action)=>{


switch (action.type){
    case "add":{
        // return 
    }
    default : {
        return store
    }
}

}

export  {reducer}